package ListsLAB;

import java.util.ArrayList;
import java.util.List;
import java.util.Scanner;

public class ex3_MergingLists {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

       /*
        Integer numbers1 = Integer.parseInt(scanner.nextLine());
        String numbers1 = scanner.nextLine();
       // Integer numbers2 = Integer.parseInt(scanner.nextLine());
        String numbers2 = scanner.nextLine();

        List<Integer> resultNumbers = new ArrayList<>();
        for (int i = 0; i < Math.min(numbers1.size(), numbers2.size()); i++) {
           // resultNums = nums1 + nums2;
        }

        if (numbers1.size() > numbers2.size()) {
            resultNumbers.addAll(getRemainingElements(numbers1, numbers2));
        } else if (numbers2.size() > numbers1.size()) {
            resultNumbers.addAll(getRemainingElements(numbers2, numbers1));
        }
        System.out.println(resultNumbers.toString().replaceAll("[\\[\\],]", ""));

    }

    public static List<Integer> getRemainingElements(List<Integer> longerList, List<Integer> shorterList) {
        List<Integer> nums = new ArrayList<>();
        for (int i = shorterList.size(); i < longerList.size(); i++) {
            nums.add(longerList.get(i));
        }
        return nums;

        */
    }
}
